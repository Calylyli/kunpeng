# Kunpeng HPC ZGEMM 优化记录

本文档是 ZGEMM 赛题的持续实验与智能体交接日志。

## 约束与固定参数

- 三个官方用例均为 RowMajor、NoTrans/NoTrans。
- `alpha = 1 + 0.5i`，`beta = 0.2 - 0.3i`，但实现仍从参数读取二者。
- 用例：`7427x7427x256`、`14848x14848x256`、`37360x8192x512`。
- 校验使用参考三重循环，复数绝对误差阈值为 `1e-10`。
- 官方编译不链接 BLAS，因此提交实现不能依赖外部 GEMM 库。

## 原始瓶颈

原始 `i -> j -> k` 对每个 C 元素顺序遍历 K，A 连续但 B 以整行跨度访问。固定用例的 N 很大，
B 访问几乎无法利用空间局部性；复数运算也被包含在带转置分支的内层循环中。

## 第一版方案

为 RowMajor/NoTrans/NoTrans 增加专用二维微分块，计划使用 `8 x 128` 输出 tile：

- 每个 OpenMP 任务负责一个输出 tile，避免多个线程写同一缓存行。
- tile 使用独立实部/虚部栈上累加数组，按 `k -> tile row -> tile column` 更新。
- 同一段 B 在 8 个输出行之间复用；最内层输出列连续，利于 GCC 生成 NEON。
- 每个输出元素仍严格按 k 从小到大累加，最后才计算 `alpha * sum + beta * C`，尽量保持与
  参考实现相同的舍入顺序，满足严格阈值。
- 非官方参数组合保留通用实现作为功能回退。

后续必须比较不同 M/N/K 形状下的 tile 大小，并以三项总耗时选择，不只看单项。

### 第一版实测与撤回方案

`8 x 128` 实部/虚部分离栈数组版本的小规模最大误差为 `9.53e-14`；第一项官方尺寸为
`2545.05 ms / 44.3875 GFLOPS`，最大误差 `7.63e-13`。编译器只确认最终写回向量化。
改为 C99 复数累加数组后，第一项退化至 `3734.97 ms / 30.2461 GFLOPS`；GCC 报告核心循环
不支持 `complex double` 向量化，因此撤回。AArch64 NEON 4x8 候选为 `3645.88 ms / 30.9852
GFLOPS`，虽通过但 32 个累加向量耗尽全部 SIMD 寄存器并产生溢出。当前收缩为 2x8，以便
16 个累加向量、B 向量和临时量同时驻留寄存器。
2x8 实测进一步退化到 `5895.62 ms / 19.1614 GFLOPS`。结论是小 tile 重复加载大跨度的
交错 B 才是主瓶颈。当前方案改为每次调用先将 B 一次性拆成连续实部/虚部面板，再使用 8x128
tile；打包为 O(KN)，而固定用例计算为 O(MKN)，可由巨大的 M 摊薄，并使核心更新真正 SIMD。

### B 打包版 tile 调参（第一项官方尺寸）

所有候选均通过，最大误差 `7.63e-13`。主要结果：8x128 为 51.48 GFLOPS，16x128 为
64.84 GFLOPS，32x64 为 72.02 GFLOPS，32x128 为 73.22 GFLOPS，64x32 为 **76.76
GFLOPS**，64x64 为 75.34 GFLOPS。默认选择 64x32：实/虚累加数组合计约 32 KiB，适合
64 KiB L1D，同时每次 B 面板加载复用于 64 个 A 行。第一项耗时 `1471.77 ms`。

### 当前提交版完整结果

默认 tile 为 64x32，38 个 OpenMP 线程，未使用 `numactl` 的本机结果：

| M | N | K | 耗时 (ms) | GFLOPS | 最大误差 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 7427 | 7427 | 256 | 1471.77 | 76.7568 | 7.63e-13 |
| 14848 | 14848 | 256 | 5838.77 | 77.3294 | 7.63e-13 |
| 37360 | 8192 | 512 | 18022.40 | 69.5575 | 1.48e-12 |

三项全部 `PASS`，总耗时 **25332.94 ms**。第三项参考实现本身非常慢，完整校验运行约需数分钟。
本机单 NUMA 节点只有 24 核，38 线程会跨节点，因此正式成绩以判题机为准。

### 当前实现交接摘要

- 官方 RowMajor/NoTrans 路径先将 B 拆为连续实部/虚部面板，再执行 64x32 tile。
- 每个输出元素仍按 k 递增顺序累计，三项误差均远小于 `1e-10`。
- 非官方参数组合保留通用正确性回退。
- 失败尝试包括 C99 复数数组、4x8 NEON 和 2x8 NEON，原因及性能已记录，不应恢复。

## 第二轮：分形状 NEON 微内核

2026-07-30 在当前容器可用的 NUMA 2、11 核上建立旧版基线：三项分别为
2436.87/8953.45/25537.09 ms，总计 **36927.41 ms**，全部 PASS。

在已有 B 实虚部分离打包上增加 AArch64 `6x4` 复数 NEON 微内核。24 个实/虚累加向量
驻留寄存器，B 的 4 列在 6 个 A 行间复用；外层使用 `60x16` 二级任务块。4x4 只有
61.74 GFLOPS，6x4 达 69.08 GFLOPS；7x4 因寄存器溢出（汇编栈向量访问从 13 增至 39）
回退到 68.26 GFLOPS。把 B 转置打包成 K 连续面板也没有收益，已撤回。

新微内核对第一项有明显收益，但第二项大输出矩阵从约 9 秒退化到约 13 秒。因此最终按输出
规模分派：`M*N <= 100000000` 使用 6x4 寄存器微内核；更大的两个官方形状继续使用原有
64x32 缓存分块。32x64/64x32/128x16 大块候选中，第三项 32x64 仅快约 0.55%，但第二项
慢约 3.4%，所以保留总耗时更稳健的 64x32。

最终 11 核、NUMA 2 官方 benchmark 结果：

| M | N | K | 耗时 (ms) | GFLOPS | 最大误差 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 7427 | 7427 | 256 | 1634.82 | 69.1012 | 3.22e-12 |
| 14848 | 14848 | 256 | 8907.21 | 50.6902 | 7.63e-13 |
| 37360 | 8192 | 512 | 25945.65 | 48.3161 | 1.17e-12 |

三项全部 PASS，总耗时 **36487.68 ms**，相对同环境旧版下降约 **1.2%**。第一项耗时下降
32.9%；第二、第三项保留大块路径，其单次差异主要处于运行波动范围。NEON FMA 改变了第一项
最后几位舍入，但最大误差仍比 `1e-10` 阈值低约 31 倍。

## 提交包规则

- ZGEMM 必须单独打包为仅有一个后缀的 `zgemm.zip`，不得使用多点文件名。
- zip 内只放 ZGEMM 工程编译所需文件，不能混入其他算子或根目录实验日志。
- 上传前用户需在平台界面勾选同意《鲲鹏开发者生态隐私声明》。该账户操作不由代码完成。
