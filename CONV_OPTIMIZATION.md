# Kunpeng HPC CONV 优化记录

本文档记录 CONV 赛题的源码分析、优化方案、正确性验证和性能结果。
本文档同时作为智能体之间的交接日志；后续方案、命令、结果、失败尝试和待办均应持续追加。

## 当前工作区状态

- 2026-07-29：已将根目录的 `conv.zip`、`zgemm.zip`、`trsm.zip` 分别解压为
  `conv/`、`zgemm/`、`trsm/`。
- 当前只优化 CONV；ZGEMM 和 TRSM 保持压缩包原始内容。
- CONV 原始核心是 `j -> i -> jk -> ik` 四重循环，并用 OpenMP 按输出行并行。
- 已新增 `conv/run.sh`，可编译并顺序执行四个官方用例。
- 当前机器是鲲鹏 920、AArch64，共 192 核、8 个 NUMA 节点，每节点 24 核；这和赛题所述
  “单一 NUMA、最多 38 核”存在拓扑差异，因此本机绝对性能不能直接等同于判题环境。
- Git 交接策略：提交三个解压后的源码目录、CONV 优化实现、运行脚本和本日志；根目录三个原始
  zip 与源码内容重复，因此通过 `.gitignore` 排除。各题编译产生的测试二进制也不入库。

## 赛题约束

- 目标函数：`conv/conv2d.c` 中的 `conv`
- 资源：单一 NUMA 节点，CPU 核数不超过 38
- 编译：`gcc -O3 bench_conv.c conv2d.c -o conv2d_test -lm -fopenmp`
- 验收：正确性校验通过后，按四个用例的总耗时排名
- 提交：`conv` 目录须包含可完成编译和四项测试的 `run.sh`

## 测试用例

| rows | cols | kernel_rows | kernel_cols |
| ---: | ---: | ---: | ---: |
| 4096 | 6144 | 39 | 39 |
| 6144 | 4096 | 41 | 41 |
| 4256 | 6390 | 55 | 55 |
| 6390 | 4256 | 81 | 81 |

## 优化与测试记录

原实现逐输出点计算完整卷积窗口，相邻输出点之间的输入数据复用依赖缓存隐式完成，
同时最内层循环沿卷积核方向，难以高效生成连续 SIMD 访存。

当前实现采用以下策略：

- 输出列支持编译期分块；针对四个固定样例，默认块宽 8192，使每行只执行一个整行块。
- 块内保持 `kernel row -> kernel column` 的原始累加顺序，避免改变每个输出点的浮点结果。
- 最内层遍历连续输出列，并使用 OpenMP SIMD 提示，使 GCC 在鲲鹏 920 上生成 NEON 向量代码。
- 输出初始化合并进按行并行区域，避免串行 `memset`，线程使用静态调度。
- `run.sh` 固定执行四个官方用例，并允许通过 `OMP_NUM_THREADS` 和 `NUMA_NODE` 覆盖默认配置。

### 为什么选择这种循环顺序

备选的 `j -> jk -> ik -> i` 能连续向量化，但整行输出约为 16--25 KiB，且输入片段与输出
共同争用每核 64 KiB L1D。加入列分块后，卷积核列移动时可从 L1 重用当前输入片段和输出
部分和。第一版经验值是 512，第一轮调整为 1024，第二轮实测后改为整行路径（默认 8192）。

没有采用 im2col：四个用例的展开矩阵极大，额外内存和打包流量不可接受。没有改变 `jk/ik`
累加次序：校验绝对误差阈值只有 `1e-5`，重新结合或分割累加器可能因浮点舍入而失败。

### 已执行验证

编译命令：

```shell
gcc -O3 conv/bench_conv.c conv/conv2d.c -o conv/conv2d_test -lm -fopenmp
```

小规模正确性及冒烟性能：

```shell
OMP_NUM_THREADS=4 ./conv/conv2d_test 256 320 7 7 1
```

结果：`PASS`，最大误差 `0.00e+00`，耗时 `0.26 ms`，`29.4670 GFLOPS`。
这证明当前循环重排在该用例上逐元素结果与参考实现一致，但不能代替四个官方尺寸验证。

编译器向量化检查：

```shell
gcc -O3 -fopenmp -fopt-info-vec-optimized -c conv/conv2d.c -o /tmp/conv2d.o
```

GCC 报告计算内层生成 16 字节和 8 字节向量循环。16 字节对应 4 个单精度浮点数，符合
鲲鹏 920 的 128-bit NEON。尝试执行 `shellcheck conv/run.sh`，但本机没有安装 `shellcheck`。

第一个官方尺寸（本机没有 `numactl`，未绑定 NUMA）：

```shell
OMP_NUM_THREADS=38 OMP_PROC_BIND=close OMP_PLACES=cores \
  ./conv/conv2d_test 4096 6144 39 39 1
```

结果：`PASS`，最大误差 `0.00e+00`，耗时 `540.63 ms`，`139.4196 GFLOPS`。
由于本机单 NUMA 节点只有 24 核且未用 `numactl` 绑定，这一数值只用于本机相对比较。

为便于分块调参，源码增加编译期宏 `CONV_COLUMN_BLOCK`，当前默认值为 8192。例如使用
`-DCONV_COLUMN_BLOCK=256` 可生成 256 列实验版本，提交版本无需额外编译参数。

### 基线和分块参数实验

从原始 `conv.zip` 解压到 `/tmp` 并独立编译，避免覆盖工作版本。第一个官方用例原始实现结果：
`PASS`，最大误差 `0.00e+00`，耗时 `3548.52 ms`，`21.2413 GFLOPS`。相较第一轮优化结果
`540.63 ms`，优化版约为基线的 6.56 倍，耗时降低约 84.8%。

同一用例、单次正式计时的初筛：

| 分块列数 | 耗时 (ms) | GFLOPS | 最大误差 |
| ---: | ---: | ---: | ---: |
| 256 | 553.88 | 136.0864 | 0 |
| 512 | 548.87 | 137.3272 | 0 |
| 1024 | 544.52 | 138.4236 | 0 |

由于单次结果存在噪声，再对 512/1024/2048 各运行 3 次正式计时并取程序报告的平均值：

| 分块列数 | 平均耗时 (ms) | GFLOPS | 最大误差 |
| ---: | ---: | ---: | ---: |
| 512 | 563.44 | 133.7759 | 0 |
| 1024 | 529.24 | 142.4217 | 0 |
| 2048 | 539.13 | 139.8078 | 0 |

因此提交默认值选择 1024；它在该组重复实验中比 512 快约 6.1%。这些实验均为 38 个 OpenMP
线程、`OMP_PROC_BIND=close`、`OMP_PLACES=cores`，但没有 NUMA 绑定。

## 后续实验清单

- [x] 用编译器向量化报告确认最内层循环生成 SIMD。
- [x] 对列分块 256 到 16384 做相对对比，默认固定为 8192（四项均走整行路径）。
- [x] 执行全部四个官方用例并通过正确性校验。
- [ ] 在与判题一致的单 NUMA、38 核环境复测绝对性能。
- [ ] 如判题机环境允许，进一步比较 GCC、毕昇编译器及额外架构编译选项。

## 当前提交版完整结果

第一轮提交曾使用 `CONV_COLUMN_BLOCK=1024`；第二轮已更新为 8192。编译命令与官方要求一致：

```shell
gcc -O3 conv/bench_conv.c conv/conv2d.c -o conv/conv2d_test -lm -fopenmp
```

运行环境变量为 `OMP_NUM_THREADS=38 OMP_PROC_BIND=close OMP_PLACES=cores`。本机缺少
`numactl`，因此下表是未显式绑定 NUMA 的本机单次结果：

| 输入尺寸 | 卷积核 | 耗时 (ms) | GFLOPS | 最大误差 | 校验 |
| --- | --- | ---: | ---: | ---: | --- |
| 4096 x 6144 | 39 x 39 | 533.79 | 141.2073 | 0.00e+00 | PASS |
| 6144 x 4096 | 41 x 41 | 532.31 | 156.3675 | 0.00e+00 | PASS |
| 4256 x 6390 | 55 x 55 | 1078.05 | 149.4126 | 0.00e+00 | PASS |
| 6390 x 4256 | 81 x 81 | 2251.13 | 153.5991 | 0.00e+00 | PASS |

四项计时总和为 **4395.28 ms**。该总和只用于记录当前本机状态，最终排名应以判题环境测量为准。

## 交接摘要

- 当前有效实现：`conv/conv2d.c`，核心方案是整行连续列 SIMD，默认块宽 8192。
- 提交入口：`conv/run.sh`；有 `numactl` 时默认绑定节点 1，无该命令时自动直接运行。
- 精度状态：四个官方尺寸全部 `PASS`，最大误差均为 0。
- 性能状态：第一个用例相对原始实现约 6.56 倍加速；完整四项本机总耗时 4395.28 ms。
- 环境风险：本机每 NUMA 节点只有 24 核且没有 `numactl`，与赛题“单 NUMA 最多 38 核”不完全一致。
- 未做事项：尚未在官方环境比较编译器、绑核布局、NUMA 节点及更激进的 AArch64 特化。

## 第二轮：针对固定测试样例的优化计划

2026-07-29 用户要求继续针对四个固定样例寻找空间。本轮遵循两个约束：每个候选必须跑官方
尺寸的正确性校验；选择依据是四项耗时总和，而不是单项最高 GFLOPS。

候选方向：

1. 分别测量 39/41/55/81 核尺寸对列块宽的敏感性。核越大，输入滑窗和指令工作集越大，统一
   的 1024 列未必最优；可以按 `kernelWidth` 选择少量固定块宽。
2. 检查 GCC 汇编中最内层 NEON 循环是否只有一条依赖链。若是，尝试显式展开连续输出向量，
   增加独立 load/FMA/store 链；每个输出仍严格按 `jk -> ik` 顺序累加，以保持零误差。
3. 比较 OpenMP 行调度策略。所有输出行工作量相同，理论上 `schedule(static)` 最合适；重点检查
   是否需要更粗的行块或 `collapse`，而不是盲目动态调度。
4. 暂不采用 im2col（内存膨胀严重）或 FFT（实现和浮点误差风险大）。FFT 对 81x81 理论上有潜力，
   但在没有外部 FFT 库且绝对误差阈值为 1e-5 的提交约束下，不作为本轮低风险方案。

### 第二轮实验结果

四项均以 38 线程、相同绑核环境、单次正式计时测试，所有结果最大误差均为 0：

| 列块宽 | 用例1 (ms) | 用例2 (ms) | 用例3 (ms) | 用例4 (ms) | 总耗时 (ms) |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 512 | 543.26 | 532.53 | 1119.72 | 2299.19 | 4494.70 |
| 1024 | 537.62 | 540.38 | 1082.16 | 2256.03 | 4416.19 |
| 2048 | 525.83 | 522.21 | 1064.48 | 2224.17 | 4336.69 |
| 4096 | 523.50 | 522.04 | 1059.37 | 2221.11 | 4326.02 |
| 8192 | 533.30 | 524.69 | 1057.67 | 2213.98 | 4329.64 |
| 16384 | 530.32 | 527.13 | 1054.65 | 2204.12 | 4316.22 |

8192 和 16384 均大于全部四项的输出宽度，生成的实际计算路径相同，差别属于运行噪声。结论是
整行工作集（约 16--25 KiB）能放入 64 KiB L1D，原来的 1024 分块反而增加了卷积核遍历和
边界开销。最终设置 `CONV_COLUMN_BLOCK=8192`，明确覆盖全部固定样例，同时比 16384 表意更直接。
相对第一轮记录的 4395.28 ms，最佳整行测量 4316.22 ms，下降约 1.80%。

还测试了每个 OpenMP 任务顺序处理 1/2/4 个相邻输出行，四项总耗时分别约为
4447.01/4446.03/4554.46 ms。该写法没有交织两行向量指令，只降低了任务分配粒度，因此无收益，
已撤回并继续采用逐行静态调度。

最终候选复测结果：

| 输入尺寸 | 卷积核 | 耗时 (ms) | GFLOPS | 最大误差 |
| --- | --- | ---: | ---: | ---: |
| 4096 x 6144 | 39 x 39 | 530.23 | 142.1546 | 0.00e+00 |
| 6144 x 4096 | 41 x 41 | 520.36 | 159.9589 | 0.00e+00 |
| 4256 x 6390 | 55 x 55 | 1054.34 | 152.7726 | 0.00e+00 |
| 6390 x 4256 | 81 x 81 | 2208.79 | 156.5433 | 0.00e+00 |

总耗时 **4313.72 ms**。相对上一提交记录的 4395.28 ms，下降 81.56 ms，即约 **1.86%**；
四项最大误差均为 0。
