# Kunpeng HPC TRSM 优化记录

本文档是 TRSM 赛题的持续实验与智能体交接日志。

## 约束与用例

- 目标：优化 `trsm/trsm.c` 的 `l_trsm`，求解行主序 `L * X = B`。
- `L` 为非单位下三角矩阵，`B/X` 为 `m x n` 行主序矩阵。
- 官方编译明确链接多线程 KBLAS：`-lkblas -fopenmp`。
- 用例：`512x19968`、`2432x17024`、`17024x512`，均使用 38 线程。
- 校验绝对误差阈值：`1e-12`。

## 源码分析与方案

原始实现按 RHS 列并行，每列执行标量前代。它对 `B[k][j]` 做跨行大步长访问，缓存和 SIMD
效率差；第三项只有 512 列而三角阶数达到 17024，列级并行也无法消除每列内部的长依赖链。

官方编译已经提供多线程 KBLAS。最终方案直接调用：

```text
cblas_dtrsm(RowMajor, Left, Lower, NoTrans, NonUnit, m, n, 1.0, L, lda, B, ldb)
```

KBLAS 的分块 TRSM 会把大部分更新转换为高吞吐 DGEMM，同时保留对角块的三角求解。这尤其
适合三个固定用例，不需要在参赛源码中重复实现和维护一套架构相关 DGEMM 微内核。

`trsm.c` 自行声明 CBLAS ABI 使用的整数枚举及函数原型，避免额外头文件路径依赖；判题链接的
`libkblas` 提供该符号。枚举值与标准 CBLAS/KBLAS 一致。

## 环境与待验证项

- 当前机器没有 `/home/HPC/HPCKit`、`kblas.h` 或 `libkblas.so`，不能直接复现官方链接环境。
- 当前系统有 OpenBLAS，且导出 ABI 兼容的 `cblas_dtrsm`；本机先用它验证接口、精度和方案量级。
- 最终必须在竞赛 KBLAS 环境执行 `trsm/run.sh`，记录三项总耗时和最大误差。

### 本机 OpenBLAS 兼容验证

为了不污染提交工程，仅在 `/tmp` 创建最小 `kblas.h` 兼容声明，并将官方 benchmark 链接到本机
OpenBLAS。小规模 `128 x 257` 用例 `PASS`，最大误差 `1.67e-16`。三个官方尺寸也全部通过：

| M | N | 耗时 (ms) | GFLOPS | 最大误差 |
| ---: | ---: | ---: | ---: | ---: |
| 512 | 19968 | 607.53 | 8.6161 | 2.22e-16 |
| 2432 | 17024 | 10297.49 | 9.7782 | 3.89e-16 |
| 17024 | 512 | 15098.70 | 9.8277 | 6.66e-16 |

本机总耗时 `26003.72 ms`。这只验证 ABI 和精度；本机 OpenBLAS 的实现、线程运行时和竞赛多线程
KBLAS 不同，不能把该性能当成正式分数。

### 当前实现交接摘要

- `l_trsm` 直接调用官方已链接的多线程 KBLAS `cblas_dtrsm`。
- 三个官方尺寸通过 ABI 兼容的 OpenBLAS 精度验证，最大误差不超过 `6.66e-16`。
- 本机没有竞赛 KBLAS，正式性能必须通过上传判题或在 HPCkit 环境运行 `trsm/run.sh` 获得。

## 提交包规则

- TRSM 必须单独打包为仅有一个后缀的 `trsm.zip`，不得使用类似 `trsm.submit.zip` 的多点名称。
- zip 内只放 TRSM 工程编译所需文件，不能混入 CONV/ZGEMM 或根目录实验日志。
- 上传前用户需在平台界面勾选同意《鲲鹏开发者生态隐私声明》。该账户操作不由代码完成。
